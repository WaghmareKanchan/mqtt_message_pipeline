# mqtt_message_pipeline
A real-time IoT pipeline — Tkinter publishes messages  to MQTT broker, Flask MVC subscribes and saves to MongoDB  via dynamic REST API. Built with Python, paho-mqtt, Flask, PyMongo.
